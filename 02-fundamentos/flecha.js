/** Con flechas, pero de forma no reducida */
/* const sumar = (a, b) => {
    return a + b;
} */

/** Con flechas pero de forma reducida */
const sumar = (a, b) => a + b;

/** Con flechas */
const saludar = () => 'Hola Mundo';
/** Sin flechas, solo la función */
function saludar() {
    return 'Hola Mundo';
}

// console.log( sumar( 5, 10 ) );
// console.log( saludar() );